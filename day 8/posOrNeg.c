#include <stdio.h>
int main()
{
    int num = 6;
    if (num > 0) // 6>0, true, so the code inside the if block will be executed
    {
        printf("The number is positive.");
    }
    else
    {
        printf("The number is negative.");
    }
}