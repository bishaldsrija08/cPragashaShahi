#include <stdio.h>
#include <stdbool.h>
int main()
{
    bool isStudent = true;
    bool hasId = false;
    printf("Is Student AND Has ID: %d\n", isStudent && hasId); // Logical AND
    printf("%d", 10 > 5);
    return 0;
}