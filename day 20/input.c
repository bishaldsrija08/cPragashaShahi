// Taking input from the user
#include<stdio.h>
int main(){
    int a;
    printf("Enter a number: ");
    scanf("%d", &a); // &a is the address of the variable a, where the input will be stored
    
    return 0;
}